#include <stddef.h>

double rpn_calc(char *input, size_t max);
// Return evaluation of Reverse Polish Notation expression from input string.
// Provide number of operands as "max" (size of memory allocated for stack).
